# Changelog — Open Music

所有版本的更新说明按倒序排列，最新版本在最前。

---

## v1.0.2 (2026-07-29)

### ✨ 新功能

#### 🎛️ 均衡器（EQ）音效系统
- 全新「均衡器」页面，支持 5 频段独立调节（60Hz / 230Hz / 910Hz / 3.6kHz / 14kHz），范围 -10dB ~ +10dB
- 内置 6 套音效预设：**默认 / 流行 / 古典 / 电子 / 人声增强 / 低音增强**
- 调整结果自动持久化，重启 App 后完整恢复
- 在「设置 → 播放控制 → 均衡器（EQ）」入口，显示当前激活预设名称
- 设备不支持硬件 EQ 时，自动降级并显示提示，**不会崩溃**

#### 🌙 睡眠定时器
- 新增「睡眠定时器」功能，在「设置 → 播放控制」中点击进入
- 支持 4 档定时：15 分钟 / 30 分钟 / 45 分钟 / 60 分钟
- 支持「当前曲结束后自动停止」模式
- 定时器激活时，入口行实时显示倒计时（如「剩余 23:45」）
- 随时可一键取消定时

### 🐛 修复 & 优化

#### 🎵 歌词同步精度提升
- 歌词时间戳存储从 `Float`（秒）升级为 `Long`（毫秒），彻底消除浮点精度累积误差
- 歌词高亮判断和点击跳转的时间精度提升至毫秒级，同步更加精准

---

## v1.0.1 (2026-07-21)

### 🔧 构建与发布

- 优化 GitHub Actions CI/CD 流水线
  - 修复 `GITHUB_TOKEN` 缺少 Release 写入权限的问题
  - 修复 Android `build-tools;29.0.3` 缺失错误，升级至 `34.0.0`
  - 修复 `package-lock.json` 未追踪导致 npm 缓存失败的问题
  - 移除 `tauri-action` 中重复的 Release 创建步骤，统一由 `publish-release` Job 汇总发布
  - 新增 Rust 构建缓存（`Swatinem/rust-cache`），大幅缩短 Windows 端构建时间
- 多端统一发布：Android APK（带签名）+ Windows MSI / EXE 安装包同步挂载至 GitHub Release
- 新增双层 OTA 热更新系统（`version.json` 远程版本检测 + APK 流式下载）

---

## v1.0.0 (2026-07-01)

### 🎉 首次发布

- **极奢 HSL 调色板引擎**：根据歌曲 Hash 动态调配主题色，切歌时以 1.2 秒 FastOutSlowIn 曲线平滑过渡
- **影院焦深歌词滚动**：歌词字重与透明度随距当前行物理距离做 3D 悬浮淡出，内置 3.5 秒手动锁定机制
- **Jetpack Media3 音频引擎**：原生 ExoPlayer 后台保活，系统锁屏、通知栏、耳机键完整同步
- **多播放模式**：列表循环 / 随机播放 / 单曲循环，与系统 MediaSession 完全同步
- **Meting API 双线路**：兼容祈杰丫（qijieya）与 Mikus 两条解析线路，可在设置页一键切换
- **歌单收藏管理**：支持收藏多个歌单，快速切换，持久化存储
- **HSL 动态主题开关**：可在设置中切换为极简纯白皇家蓝静态主题
- PWA 网页端 / Android 原生 / Windows 桌面端（Tauri）/ 鸿蒙原生端 多端支持
